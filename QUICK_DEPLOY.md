# ⚡ Быстрый деплой на Vercel

## 🎯 Самый быстрый способ (3 команды)

```bash
# 1. Установите Vercel CLI (если еще нет)
npm i -g vercel

# 2. Войдите в аккаунт
vercel login

# 3. Деплой!
./deploy.sh
```

Или вручную:
```bash
vercel --prod
```

## 📋 Что нужно сделать ПОСЛЕ первого деплоя

### 1. Настройте базу данных (выберите один вариант):

#### Вариант А: Vercel Postgres (рекомендуется)
1. Откройте ваш проект в Vercel Dashboard
2. Storage → Create Database → Postgres
3. Vercel автоматически добавит `DATABASE_URL`

#### Вариант Б: Supabase (бесплатно)
1. Создайте проект на [supabase.com](https://supabase.com)
2. Settings → Database → Connection string (URI)
3. Добавьте в Vercel: Settings → Environment Variables
   ```
   DATABASE_URL=postgresql://postgres:[PASSWORD]@[HOST]:5432/postgres
   ```

#### Вариант В: Neon (serverless)
1. Создайте проект на [neon.tech](https://neon.tech)
2. Скопируйте connection string
3. Добавьте в Vercel Environment Variables

### 2. Добавьте переменные окружения в Vercel

Settings → Environment Variables:

```
DATABASE_URL=postgresql://...  (из шага 1)
JWT_SECRET=ваш_секретный_ключ_минимум_32_символа_случайная_строка
NODE_ENV=production
```

Генерация JWT_SECRET:
```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

### 3. Примените миграции БД

```bash
# Скачайте production переменные
vercel env pull .env.production.local

# Примените миграции
cd backend
npx prisma migrate deploy

# Добавьте тестовые данные (опционально)
npx prisma db seed
```

### 4. Проверьте деплой

Откройте ваш Vercel URL и проверьте:
- ✅ Главная страница загружается
- ✅ Можно зарегистрироваться
- ✅ Можно войти
- ✅ Профиль работает

## 🔧 Локальное тестирование production билда

```bash
# Создайте билд
npm run build

# Запустите локально
vercel dev
```

## 🐛 Частые проблемы

### "Cannot find module '@prisma/client'"
```bash
cd backend
npx prisma generate
vercel --prod
```

### "Database connection failed"
- Проверьте `DATABASE_URL` в Vercel Environment Variables
- Убедитесь что БД доступна из интернета
- Для Supabase добавьте `?sslmode=require` в конец URL

### "CORS error"
- Проверьте что `backend/src/app.ts` обновлен (должен разрешать `.vercel.app`)
- Перезапустите деплой: `vercel --prod`

### API не отвечает
- Проверьте логи: Vercel Dashboard → Functions → Logs
- Убедитесь что `backend/api/index.ts` существует
- Проверьте `vercel.json` конфигурацию

## 📊 Мониторинг

После деплоя можете отслеживать:
- **Логи**: Vercel Dashboard → Functions → Logs
- **Аналитика**: Vercel Dashboard → Analytics
- **Ошибки**: Vercel Dashboard → Functions → Errors

## 🔄 Обновление проекта

После изменений в коде:
```bash
git add .
git commit -m "Update features"
git push

# Vercel автоматически задеплоит, или вручную:
vercel --prod
```

## 💡 Полезные команды

```bash
# Посмотреть список деплоев
vercel ls

# Посмотреть логи
vercel logs

# Скачать production env переменные
vercel env pull

# Удалить деплой
vercel rm [deployment-url]
```

---

**Готово!** Ваш проект на Vercel 🎉

Нужна помощь? Смотрите подробную инструкцию в `VERCEL_DEPLOY.md`
