# 🚀 Деплой на Vercel

## Быстрый старт

### 1. Установите Vercel CLI (если еще не установлен)
```bash
npm i -g vercel
```

### 2. Войдите в аккаунт
```bash
vercel login
```

### 3. Деплой проекта
```bash
vercel
```

Для production деплоя:
```bash
vercel --prod
```

## ⚙️ Настройка переменных окружения в Vercel

После первого деплоя, настройте переменные окружения в дашборде Vercel:

### Backend переменные:
1. Перейдите в Settings → Environment Variables
2. Добавьте следующие переменные:

```
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
JWT_SECRET=ваш_секретный_ключ_минимум_32_символа
PORT=3001
NODE_ENV=production
```

### Где взять DATABASE_URL?

**Рекомендуемые провайдеры PostgreSQL:**
- **Vercel Postgres** (интеграция в один клик)
- **Supabase** (бесплатный tier)
- **Neon** (serverless PostgreSQL)
- **Railway** (простая настройка)

#### Пример с Vercel Postgres:
1. В дашборде проекта → Storage → Create Database
2. Выберите Postgres
3. Vercel автоматически добавит `DATABASE_URL`

#### Пример с Supabase:
1. Создайте проект на supabase.com
2. Settings → Database → Connection string
3. Скопируйте URI и добавьте в Vercel

## 📦 Что происходит при деплое

1. **Frontend** (Vite) билдится в статические файлы
2. **Backend** (Express) деплоится как serverless функция
3. **Prisma** автоматически генерирует клиент
4. **Миграции БД** нужно запустить вручную (см. ниже)

## 🗄️ Миграции базы данных

После настройки `DATABASE_URL`, выполните миграции:

```bash
# Локально с production DATABASE_URL
cd backend
DATABASE_URL="ваш_production_url" npx prisma migrate deploy
DATABASE_URL="ваш_production_url" npx prisma db seed
```

Или через Vercel CLI:
```bash
vercel env pull .env.production
cd backend
npx prisma migrate deploy
npx prisma db seed
```

## 🔧 Локальное тестирование production билда

```bash
# Билд фронтенда
npm run build

# Запуск локально
vercel dev
```

## 📝 Важные моменты

1. **CORS**: Backend настроен на работу с любым origin в production
2. **API Routes**: Все запросы к `/api/*` идут на backend
3. **Static Files**: Все остальное отдается как SPA
4. **Environment**: Не забудьте добавить все переменные из `.env.example`

## 🐛 Troubleshooting

### Ошибка "Cannot find module '@prisma/client'"
```bash
cd backend
npx prisma generate
```

### Ошибка подключения к БД
- Проверьте `DATABASE_URL` в Vercel Environment Variables
- Убедитесь, что БД доступна из интернета
- Проверьте SSL настройки (добавьте `?sslmode=require` если нужно)

### Frontend не видит API
- Проверьте, что в `src/lib/api.ts` правильный базовый URL
- В production должно быть `/api` (относительный путь)

## 🎯 Checklist перед деплоем

- [ ] Все тесты проходят (`npm test` и `cd backend && npm test`)
- [ ] `.env.example` актуален
- [ ] `DATABASE_URL` настроен в Vercel
- [ ] `JWT_SECRET` установлен (минимум 32 символа)
- [ ] Миграции применены к production БД
- [ ] CORS настроен правильно
- [ ] Проверен production билд локально (`vercel dev`)

## 🌐 После деплоя

Vercel выдаст вам URL вида: `https://your-project.vercel.app`

Проверьте:
1. Главная страница загружается
2. Регистрация/логин работает
3. API отвечает на `/api/health` или `/api/auth/me`
4. Загрузка файлов работает (если используется)

---

**Готово!** Ваш проект теперь доступен онлайн 🎉
