# 🚀 Деплой на Vercel - Начните здесь!

## ⚡ Самый быстрый способ (5 минут)

### Шаг 1: Установите Vercel CLI
```bash
npm i -g vercel
```

### Шаг 2: Войдите в аккаунт
```bash
vercel login
```

### Шаг 3: Запустите деплой
```bash
./deploy.sh
```

Или вручную:
```bash
vercel --prod
```

### Шаг 4: Настройте базу данных

После деплоя откройте Vercel Dashboard:

1. **Storage → Create Database → Postgres**
2. Vercel автоматически добавит `DATABASE_URL`

### Шаг 5: Добавьте JWT_SECRET

В Vercel Dashboard → Settings → Environment Variables:

```bash
# Сгенерируйте секретный ключ
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

# Добавьте в Vercel:
JWT_SECRET=полученная_строка
```

### Шаг 6: Примените миграции

```bash
vercel env pull .env.production.local
cd backend
npx prisma migrate deploy
npx prisma db seed
```

### Шаг 7: Готово! 🎉

Откройте ваш Vercel URL и проверьте работу!

---

## 📚 Дополнительная документация

- **QUICK_DEPLOY.md** - быстрая инструкция с решением проблем
- **VERCEL_DEPLOY.md** - подробная документация
- **DEPLOY_CHECKLIST.md** - чеклист для проверки

---

## 🆘 Нужна помощь?

### База данных не подключается?
→ Смотрите раздел "Troubleshooting" в `QUICK_DEPLOY.md`

### CORS ошибки?
→ Убедитесь что `backend/src/app.ts` обновлен (уже сделано!)

### API не отвечает?
→ Проверьте логи: `vercel logs`

---

**Все готово к деплою!** Просто запустите `./deploy.sh` 🚀
