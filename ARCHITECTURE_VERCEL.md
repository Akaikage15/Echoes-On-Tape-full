# 🏗️ Архитектура на Vercel

## Как работает ваш проект на Vercel

```
┌─────────────────────────────────────────────────────────────┐
│                    Vercel Edge Network                       │
│                  (CDN + Load Balancer)                       │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
                    ┌─────────────────┐
                    │   Routing Layer  │
                    │  (vercel.json)   │
                    └─────────────────┘
                              │
                ┌─────────────┴─────────────┐
                │                           │
                ▼                           ▼
    ┌───────────────────┐       ┌──────────────────────┐
    │  Static Files     │       │  Serverless Function │
    │  (Frontend SPA)   │       │  (Backend API)       │
    │                   │       │                      │
    │  • React          │       │  • Express.js        │
    │  • Vite build     │       │  • Prisma ORM        │
    │  • /dist folder   │       │  • JWT Auth          │
    └───────────────────┘       └──────────────────────┘
            │                              │
            │                              ▼
            │                   ┌──────────────────┐
            │                   │   PostgreSQL     │
            │                   │   Database       │
            │                   │                  │
            │                   │  • Vercel DB     │
            │                   │  • Supabase      │
            │                   │  • Neon          │
            │                   └──────────────────┘
            │
            ▼
    ┌───────────────────┐
    │   User Browser    │
    │                   │
    │  • React App      │
    │  • API calls      │
    └───────────────────┘
```

## Маршрутизация запросов

### Frontend запросы (статика)
```
User → https://your-app.vercel.app/
     → Vercel CDN
     → /dist/index.html
     → React SPA загружается
```

### API запросы
```
User → https://your-app.vercel.app/api/auth/login
     → Vercel Edge
     → backend/api/index.ts (serverless function)
     → Express.js обрабатывает запрос
     → Prisma → PostgreSQL
     → Ответ пользователю
```

## Процесс деплоя

```
1. Локальная машина
   └─> git push / vercel --prod
        │
        ▼
2. Vercel Build System
   ├─> npm run build
   │   ├─> vite build (frontend)
   │   └─> cd backend && npm install && prisma generate
   │
   ├─> Создание serverless функции из backend/api/index.ts
   │
   └─> Деплой на Edge Network
        │
        ▼
3. Production
   ├─> Static files → CDN
   ├─> Serverless function → Lambda-like environment
   └─> Database → PostgreSQL (external)
```

## Переменные окружения

### Frontend (.env.production)
```
VITE_API_BASE_URL=/api
```
→ В production API доступен по относительному пути

### Backend (Vercel Environment Variables)
```
DATABASE_URL=postgresql://...
JWT_SECRET=...
NODE_ENV=production
```
→ Настраиваются в Vercel Dashboard

## Особенности Serverless

### ✅ Преимущества
- **Автоматический скейлинг** - выдерживает любую нагрузку
- **Оплата за использование** - платите только за запросы
- **Глобальное распределение** - быстро работает везде
- **Нет управления серверами** - Vercel все делает за вас

### ⚠️ Ограничения
- **Cold start** - первый запрос может быть медленнее (~1-2 сек)
- **Timeout** - максимум 10 секунд на запрос (настроено в vercel.json)
- **Memory** - 1024 MB (настроено в vercel.json)
- **Stateless** - нет постоянного состояния между запросами

### 💡 Оптимизации
- Prisma Client генерируется при билде
- Connection pooling для БД
- CORS настроен для Vercel domains
- Логи через Winston

## Мониторинг

### Vercel Dashboard
```
Project → Functions
├─> Logs - все логи backend
├─> Errors - ошибки в реальном времени
├─> Performance - время выполнения
└─> Invocations - количество запросов
```

### Что отслеживать
- **Response time** - должно быть < 1 сек
- **Error rate** - должно быть < 1%
- **Cold starts** - первый запрос после простоя
- **Database connections** - не должно быть утечек

## Безопасность

### ✅ Что настроено
- CORS для Vercel domains
- JWT токены для аутентификации
- Refresh tokens в localStorage
- HTTPS по умолчанию
- Environment variables защищены

### 🔒 Рекомендации
- Используйте сильный JWT_SECRET (32+ символа)
- Настройте rate limiting (опционально)
- Включите Vercel Authentication (опционально)
- Регулярно обновляйте зависимости

## Стоимость

### Vercel Free Tier
- ✅ 100 GB bandwidth
- ✅ Unlimited deployments
- ✅ Automatic HTTPS
- ✅ Preview deployments
- ✅ Analytics

### Vercel Postgres Free Tier
- ✅ 256 MB storage
- ✅ 60 hours compute time/month
- ✅ Automatic backups

### Альтернативы БД (бесплатно)
- **Supabase**: 500 MB storage, 2 GB bandwidth
- **Neon**: 3 GB storage, 100 hours compute

## Обновление проекта

```bash
# Автоматический деплой при push
git push origin main

# Или вручную
vercel --prod

# Preview деплой (для тестирования)
vercel
```

## Rollback (откат)

```bash
# Посмотреть список деплоев
vercel ls

# Откатиться на предыдущую версию
# В Vercel Dashboard → Deployments → Promote to Production
```

---

**Ваш проект использует современную serverless архитектуру!** 🚀
