# 🔧 Исправление ошибки 500 (FUNCTION_INVOCATION_FAILED)

## Причина ошибки

Serverless функция крашится при запуске. Скорее всего:
1. ❌ Не установлены переменные окружения (DATABASE_URL, JWT_SECRET)
2. ❌ База данных не создана
3. ❌ Prisma Client не сгенерирован

## 🔍 Шаг 1: Проверьте логи

Откройте логи функции в Vercel:
https://vercel.com/akaikages-projects/echoes-on-tape/functions

Найдите последний запрос и посмотрите ошибку. Скорее всего увидите:
- `DATABASE_URL не установлен`
- `Cannot find module '@prisma/client'`
- `Connection refused` или `ECONNREFUSED`

## ✅ Шаг 2: Создайте базу данных

### Вариант A: Vercel Postgres (быстро)

1. Откройте: https://vercel.com/akaikages-projects/echoes-on-tape/stores
2. Нажмите **"Create Database"**
3. Выберите **"Postgres"**
4. Выберите регион (например, Frankfurt)
5. Нажмите **"Create"**

Vercel автоматически добавит `DATABASE_URL` в переменные окружения.

### Вариант B: Supabase (больше места бесплатно)

1. Зарегистрируйтесь: https://supabase.com
2. Создайте новый проект
3. Перейдите в **Settings → Database**
4. Скопируйте **Connection string** (URI format)
5. Добавьте в Vercel (см. Шаг 3)

Пример строки подключения:
```
postgresql://postgres.xxxxx:password@aws-0-eu-central-1.pooler.supabase.com:5432/postgres
```

## ✅ Шаг 3: Добавьте переменные окружения

Откройте: https://vercel.com/akaikages-projects/echoes-on-tape/settings/environment-variables

### 1. DATABASE_URL

**Если используете Vercel Postgres:**
- Уже добавлен автоматически ✓

**Если используете Supabase/Neon:**
- Name: `DATABASE_URL`
- Value: `postgresql://user:password@host:5432/database`
- Environment: ✓ Production ✓ Preview ✓ Development
- Нажмите **"Save"**

### 2. JWT_SECRET

**Сгенерируйте секретный ключ:**
```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

Скопируйте результат (например: `a1b2c3d4e5f6...`)

**Добавьте в Vercel:**
- Name: `JWT_SECRET`
- Value: (вставьте сгенерированный ключ)
- Environment: ✓ Production ✓ Preview ✓ Development
- Нажмите **"Save"**

### 3. NODE_ENV

- Name: `NODE_ENV`
- Value: `production`
- Environment: ✓ Production
- Нажмите **"Save"**

## ✅ Шаг 4: Примените миграции базы данных

После создания БД и добавления переменных:

```bash
# 1. Скачайте production переменные
vercel env pull .env.production.local

# 2. Проверьте подключение
cd backend
npx prisma db pull

# 3. Примените миграции
npx prisma migrate deploy

# 4. Добавьте тестовые данные (опционально)
npx prisma db seed
```

## ✅ Шаг 5: Перезапустите деплой

После добавления переменных окружения нужно перезапустить деплой:

### Вариант A: Через Vercel Dashboard
1. Откройте: https://vercel.com/akaikages-projects/echoes-on-tape/deployments
2. Найдите последний деплой
3. Нажмите **"⋯"** → **"Redeploy"**
4. Выберите **"Use existing Build Cache"** (быстрее)
5. Нажмите **"Redeploy"**

### Вариант B: Через Git
```bash
git commit --allow-empty -m "Trigger redeploy"
git push origin main
```

### Вариант C: Через CLI
```bash
vercel --prod --force
```

## 🔍 Шаг 6: Проверьте работу

### Простой health check (без БД):
```
https://echoes-on-tape.vercel.app/api/health
```

Должен вернуть:
```json
{
  "status": "ok",
  "message": "Serverless function is working! 🚀",
  "env": {
    "hasDatabase": true,
    "hasJwtSecret": true
  }
}
```

### Полный API:
```
https://echoes-on-tape.vercel.app/api/
```

Должен вернуть:
```json
{
  "status": "ok",
  "message": "Echoes On Tape Backend is running with PostgreSQL + Prisma! 🚀"
}
```

## 🐛 Если все еще не работает

### Проверьте логи снова:
https://vercel.com/akaikages-projects/echoes-on-tape/functions

### Типичные ошибки:

#### 1. "Cannot find module '@prisma/client'"
**Решение:**
```bash
cd backend
npx prisma generate
git add -A
git commit -m "Regenerate Prisma Client"
git push
```

#### 2. "Connection refused" или "ECONNREFUSED"
**Причина:** DATABASE_URL неправильный или БД недоступна

**Проверьте:**
```bash
# Локально проверьте подключение
cd backend
npx prisma db pull
```

#### 3. "Invalid `prisma.xxx()` invocation"
**Причина:** Миграции не применены

**Решение:**
```bash
cd backend
npx prisma migrate deploy
```

#### 4. "JWT malformed"
**Причина:** JWT_SECRET не установлен или неправильный

**Проверьте:**
- JWT_SECRET установлен в Vercel
- JWT_SECRET одинаковый для всех окружений

## 📞 Дополнительная помощь

### Документация:
- `DEPLOY_CHECKLIST_FULL.md` - полный чек-лист
- `БЫСТРЫЙ_ДЕПЛОЙ.md` - краткая инструкция
- `VERCEL_SETUP_FINAL.md` - настройка Vercel

### Полезные ссылки:
- Vercel Dashboard: https://vercel.com/akaikages-projects/echoes-on-tape
- Логи функций: https://vercel.com/akaikages-projects/echoes-on-tape/functions
- Переменные окружения: https://vercel.com/akaikages-projects/echoes-on-tape/settings/environment-variables
- База данных: https://vercel.com/akaikages-projects/echoes-on-tape/stores

---

**После выполнения всех шагов проект должен работать!** 🚀
