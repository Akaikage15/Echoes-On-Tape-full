# 🎵 Echoes On Tape - Backend

Backend API для платформы Echoes On Tape, построенный на Express.js + PostgreSQL + Prisma.

## ⚠️ Требования

- **Node.js** 18.x или выше
- **PostgreSQL** 14.x или выше (обязательно!)
- **npm** или **yarn**

## 🚀 Быстрый старт

### 0. Установка PostgreSQL

**Если PostgreSQL не установлен:**

```bash
# Ubuntu/Debian
sudo apt update
sudo apt install postgresql postgresql-contrib
sudo systemctl start postgresql
```

См. подробную инструкцию в [SETUP_DATABASE.md](./SETUP_DATABASE.md)

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка базы данных

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Отредактируйте `.env` и укажите ваши данные для подключения к PostgreSQL:

```env
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/echoes_on_tape?schema=public"
JWT_SECRET=your_secret_key_here
PORT=3001
```

### 3. Запуск миграций

```bash
npm run prisma:migrate
```

### 4. Заполнение БД тестовыми данными

```bash
npx prisma db seed
```

### 5. Запуск сервера

```bash
npm start
```

Сервер будет доступен по адресу: `http://localhost:3001`

---

## 📚 Документация

- [Руководство по миграции](./MIGRATION_GUIDE.md) - подробная инструкция по переходу с in-memory на PostgreSQL
- [Схема БД](./doc/schema.md) - описание структуры базы данных

---

## 🛠️ Доступные команды

```bash
# Разработка
npm start              # Запуск сервера
npm run dev            # Запуск сервера (алиас)

# Prisma
npm run prisma:generate   # Генерация Prisma Client
npm run prisma:migrate    # Создание и применение миграций
npm run prisma:studio     # Открыть Prisma Studio (GUI для БД)

# База данных
npx prisma db seed        # Заполнить БД тестовыми данными
npx prisma migrate reset  # Сбросить БД (ОСТОРОЖНО!)
```

---

## 🔑 Тестовые аккаунты

После запуска seed скрипта доступны следующие аккаунты:

| Email | Пароль | Подписка |
|-------|--------|----------|
| admin@echoes.tape | password123 | Pro |
| test@test.com | password123 | Fan |
| free@test.com | password123 | None |

---

## 📡 API Endpoints

### Authentication
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `GET /api/auth/profile` - Получить профиль (требует токен)

### Subscriptions
- `POST /api/subscriptions/purchase` - Купить подписку (требует токен)

### Releases
- `GET /api/releases` - Список всех релизов
- `GET /api/releases/:id` - Получить релиз по ID

### Artists
- `GET /api/artists` - Список всех артистов
- `GET /api/artists/:id` - Получить артиста по ID

### Posts
- `GET /api/posts` - Список всех постов
- `GET /api/posts/:id` - Получить пост по ID

### Exclusives
- `GET /api/exclusives` - Список эксклюзивного контента
- `GET /api/exclusives/:id` - Получить эксклюзив по ID

### Merch
- `GET /api/merch` - Список товаров
- `GET /api/merch/:id` - Получить товар по ID

### Polls
- `GET /api/polls` - Список голосований
- `GET /api/polls/:id` - Получить голосование по ID
- `POST /api/polls/:id/vote` - Проголосовать (требует токен)

### Pro Library
- `GET /api/pro-library` - Список PRO-библиотеки
- `GET /api/pro-library/:id` - Получить элемент по ID

### Demos
- `GET /api/demos` - Список демо пользователя (требует токен)
- `GET /api/demos/:id` - Получить демо по ID (требует токен)
- `POST /api/demos` - Отправить демо (требует токен)
- `PUT /api/demos/:id/status` - Обновить статус демо (требует токен)

---

## 🏗️ Архитектура

```
backend/
├── prisma/
│   ├── schema.prisma       # Схема БД
│   ├── seed.ts             # Seed данные
│   └── migrations/         # История миграций
├── src/
│   ├── lib/
│   │   └── prisma.ts       # Prisma Client singleton
│   ├── repositories/       # Слой доступа к данным
│   │   ├── user.repository.ts
│   │   ├── artist.repository.ts
│   │   └── ...
│   ├── utils/
│   │   └── config.ts       # Конфигурация
│   └── index.ts            # Главный файл сервера
├── .env                    # Переменные окружения (не в git)
├── .env.example            # Пример .env
└── package.json
```

---

## 🔧 Технологии

- **Node.js** - Runtime
- **Express.js** - Web framework
- **PostgreSQL** - База данных
- **Prisma** - ORM
- **TypeScript** - Типизация
- **JWT** - Аутентификация
- **bcryptjs** - Хеширование паролей

---

## 📝 Примечания

- Все пароли хешируются с помощью bcrypt
- JWT токены действительны 1 час
- Для production обязательно измените `JWT_SECRET`
- Используйте HTTPS в production
- Настройте CORS для вашего домена

---

## 🐛 Troubleshooting

См. раздел Troubleshooting в [MIGRATION_GUIDE.md](./MIGRATION_GUIDE.md)
