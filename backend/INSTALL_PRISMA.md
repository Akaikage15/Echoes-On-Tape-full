# 🔧 Установка Prisma (решение проблем с сетью)

## Проблема

При установке Prisma может возникнуть ошибка:
```
Error: aborted
ECONNRESET
```

Это происходит из-за проблем с загрузкой Prisma engines.

## ✅ Решение: использование зеркала

### Вариант 1: Через переменную окружения (рекомендуется)

```bash
cd backend
export PRISMA_ENGINES_MIRROR=https://registry.npmmirror.com/-/binary/prisma
npm install
```

### Вариант 2: Через .npmrc

Создайте файл `backend/.npmrc`:

```
prisma_engines_mirror=https://registry.npmmirror.com/-/binary/prisma
```

Затем:
```bash
cd backend
npm install
```

### Вариант 3: Глобальная настройка npm

```bash
npm config set prisma_engines_mirror https://registry.npmmirror.com/-/binary/prisma
cd backend
npm install
```

## 📦 После успешной установки

Сгенерируйте Prisma Client:

```bash
npx prisma generate
```

Вы должны увидеть:
```
✔ Generated Prisma Client (v6.19.0) to ./node_modules/@prisma/client
```

## ✅ Проверка

Убедитесь, что ошибка исправлена:

```bash
# Проверить, что Prisma Client установлен
ls node_modules/@prisma/client

# Должны увидеть файлы и папки
```

## 🚀 Продолжение работы

После успешной установки продолжайте по [QUICK_START.md](./QUICK_START.md):

1. Настроить `.env`
2. Запустить миграции: `npm run prisma:migrate`
3. Заполнить БД: `npx prisma db seed`
4. Запустить сервер: `npm start`

## 🌐 Альтернативные зеркала

Если китайское зеркало не работает, попробуйте:

```bash
# Cloudflare
export PRISMA_ENGINES_MIRROR=https://prisma-builds.s3-eu-west-1.amazonaws.com

# GitHub (может быть медленным)
export PRISMA_ENGINES_MIRROR=https://github.com/prisma/prisma-engines/releases/download
```

## ❓ Все еще не работает?

1. Проверьте интернет-соединение
2. Попробуйте через VPN
3. Проверьте firewall/proxy настройки
4. Попробуйте позже (возможны временные проблемы с CDN)

## 📝 Примечание

После успешной установки зеркало больше не нужно - Prisma engines уже скачаны локально.
