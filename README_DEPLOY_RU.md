# 🚀 Деплой проекта на Vercel

## Что было сделано

Ваш проект полностью готов к деплою на Vercel! Вот что настроено:

### ✅ Созданные файлы

1. **vercel.json** - конфигурация для Vercel
   - Frontend билдится как статический сайт
   - Backend работает как serverless функция
   - Все `/api/*` запросы идут на backend

2. **backend/api/index.ts** - entry point для serverless функции
   - Экспортирует Express app для Vercel

3. **deploy.sh** - скрипт для быстрого деплоя
   - Проверяет тесты (опционально)
   - Создает билд
   - Запускает деплой

4. **.vercelignore** - игнорируемые файлы при деплое

5. **.env.production** - переменные для production

### ✅ Обновленные файлы

1. **package.json** - добавлены скрипты:
   - `build` - билд фронтенда + установка backend зависимостей + генерация Prisma
   - `vercel-build` - специальный скрипт для Vercel

2. **backend/src/app.ts** - обновлен CORS:
   - Разрешает все `.vercel.app` домены в production
   - Поддерживает локальную разработку

3. **.gitignore** - добавлены:
   - `.env.production.local`
   - `.vercel`

## 🎯 Как задеплоить (3 команды)

```bash
# 1. Установите Vercel CLI
npm i -g vercel

# 2. Войдите
vercel login

# 3. Деплой!
./deploy.sh
```

## 📋 После деплоя (обязательно!)

### 1. Настройте базу данных

**Самый простой способ - Vercel Postgres:**
1. Откройте проект в Vercel Dashboard
2. Storage → Create Database → Postgres
3. Готово! `DATABASE_URL` добавится автоматически

**Альтернативы:**
- Supabase (бесплатно): https://supabase.com
- Neon (serverless): https://neon.tech

### 2. Добавьте JWT_SECRET

```bash
# Сгенерируйте ключ
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

# Добавьте в Vercel Dashboard:
# Settings → Environment Variables → Add
# Name: JWT_SECRET
# Value: полученная_строка
```

### 3. Примените миграции БД

```bash
# Скачайте production переменные
vercel env pull .env.production.local

# Примените миграции
cd backend
npx prisma migrate deploy

# Добавьте тестовые данные
npx prisma db seed
```

### 4. Проверьте работу

Откройте ваш Vercel URL и проверьте:
- ✅ Регистрация
- ✅ Вход
- ✅ Профиль
- ✅ Загрузка аватара
- ✅ Социальные ссылки

## 📚 Документация

- **START_HERE.md** - начните отсюда (самая краткая инструкция)
- **QUICK_DEPLOY.md** - быстрая инструкция + решение проблем
- **VERCEL_DEPLOY.md** - подробная документация
- **DEPLOY_CHECKLIST.md** - чеклист для проверки

## 🐛 Частые проблемы

### "Cannot find module '@prisma/client'"
```bash
cd backend
npx prisma generate
vercel --prod
```

### "Database connection failed"
- Проверьте `DATABASE_URL` в Vercel Environment Variables
- Убедитесь что БД доступна из интернета

### "CORS error"
- Уже исправлено! `backend/src/app.ts` настроен правильно
- Если проблема осталась - перезапустите деплой

### API не отвечает
```bash
# Проверьте логи
vercel logs

# Проверьте что файлы на месте
ls backend/api/index.ts
cat vercel.json
```

## 🎉 Готово!

Ваш проект готов к показу заказчику!

**Следующие шаги:**
1. Запустите `./deploy.sh`
2. Настройте БД в Vercel Dashboard
3. Добавьте `JWT_SECRET`
4. Примените миграции
5. Откройте URL и проверьте работу

---

**Нужна помощь?** Смотрите подробные инструкции в других файлах!
