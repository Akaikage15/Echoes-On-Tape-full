# ✅ Чеклист деплоя на Vercel

## Перед деплоем

- [ ] Все тесты проходят
  ```bash
  npm test
  cd backend && npm test
  ```

- [ ] Локальный билд работает
  ```bash
  npm run build
  ```

- [ ] Все изменения закоммичены
  ```bash
  git status
  git add .
  git commit -m "Ready for deployment"
  ```

## Деплой

- [ ] Установлен Vercel CLI
  ```bash
  npm i -g vercel
  ```

- [ ] Выполнен вход в Vercel
  ```bash
  vercel login
  ```

- [ ] Запущен деплой
  ```bash
  ./deploy.sh
  # или
  vercel --prod
  ```

## После деплоя

### 1. База данных

- [ ] Создана PostgreSQL база (Vercel Postgres / Supabase / Neon)
- [ ] `DATABASE_URL` добавлен в Vercel Environment Variables
- [ ] Миграции применены
  ```bash
  vercel env pull .env.production.local
  cd backend
  npx prisma migrate deploy
  ```
- [ ] Seed данные загружены (опционально)
  ```bash
  npx prisma db seed
  ```

### 2. Переменные окружения

В Vercel Dashboard → Settings → Environment Variables:

- [ ] `DATABASE_URL` - connection string к PostgreSQL
- [ ] `JWT_SECRET` - случайная строка минимум 32 символа
- [ ] `NODE_ENV` - установлен в `production`

Генерация JWT_SECRET:
```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

### 3. Проверка работоспособности

Откройте ваш Vercel URL и проверьте:

- [ ] Главная страница загружается
- [ ] Регистрация работает
- [ ] Вход работает
- [ ] Профиль открывается
- [ ] Можно изменить данные профиля
- [ ] Загрузка аватара работает
- [ ] Социальные ссылки сохраняются
- [ ] Смена пароля работает
- [ ] Выход работает

### 4. API проверка

- [ ] `/api/health` или `/` отвечает
- [ ] `/api/auth/me` работает с токеном
- [ ] CORS настроен правильно (нет ошибок в консоли)

### 5. Мониторинг

- [ ] Проверены логи в Vercel Dashboard → Functions → Logs
- [ ] Нет критических ошибок
- [ ] Настроены уведомления об ошибках (опционально)

## Troubleshooting

### Если что-то не работает:

1. **Проверьте логи**
   ```bash
   vercel logs
   ```

2. **Проверьте переменные окружения**
   - Vercel Dashboard → Settings → Environment Variables
   - Убедитесь что все переменные установлены

3. **Пересоберите проект**
   ```bash
   vercel --prod --force
   ```

4. **Проверьте БД подключение**
   ```bash
   cd backend
   npx prisma db pull
   ```

5. **Проверьте CORS**
   - Откройте консоль браузера
   - Ищите ошибки CORS
   - Убедитесь что `backend/src/app.ts` обновлен

## Готово! 🎉

Ваш проект развернут на Vercel и готов к показу заказчику!

**URL проекта:** https://your-project.vercel.app

---

Нужна помощь? Смотрите:
- `QUICK_DEPLOY.md` - быстрая инструкция
- `VERCEL_DEPLOY.md` - подробная документация
