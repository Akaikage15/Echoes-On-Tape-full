# 🗺️ ROADMAP по доработке и улучшению проекта (ОБНОВЛЁННЫЙ)

## 📊 Общая стратегия

Roadmap разделен на **5 фаз** с приоритизацией по критичности для продакшн-готовности проекта.

---

## 🔴 ФАЗА 0: Критические багфиксы (Hotfix)
**Цель:** Исправить блокирующие баги в текущей версии  
**Срок:** 1-2 недели  
**Приоритет:** 🔥🔥🔥 ЭКСТРЕННО

### 0.1 Исправление личного кабинета
**Приоритет:** 🔥 КРИТИЧНО  
**Сложность:** M (3-5 дней)  
**Зависимости:** Нет

**Проблемы:**
- ❌ Не работают вкладки в личном кабинете
- ❌ Не отображается информация о подписке
- ❌ Возможны проблемы с роутингом внутри AccountPage

**Задачи:**
- [ ] **Диагностика проблемы с вкладками:**
  - Проверить `AccountPage.tsx` на ошибки в состоянии вкладок
  - Проверить правильность работы компонентов shadcn/ui Tabs
  - Проверить console на JavaScript errors
  - Добавить логирование переключения вкладок

- [ ] **Исправление отображения подписки:**
  - Проверить `useSubscription.ts` hook
  - Проверить API эндпоинт `/api/subscriptions/status`
  - Проверить маппинг данных подписки из store
  - Убедиться, что JWT токен передаётся корректно
  - Добавить fallback UI для случая отсутствия подписки

- [ ] **Тестирование исправлений:**
  - Написать unit-тесты для компонента AccountPage
  - Проверить все сценарии: с подпиской, без подписки, истёкшая подписка
  - Протестировать переключение между вкладками

**Пример исправления:**
```typescript
// src/pages/AccountPage.tsx - возможное исправление

import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";
import { useSubscription } from "@/hooks/useSubscription";
import { useStore } from "@/lib/store";

const AccountPage = () => {
  const user = useStore((state) => state.user);
  const { subscription, loading, error } = useSubscription();
  const [activeTab, setActiveTab] = useState("overview");

  // Логирование для отладки
  useEffect(() => {
    console.log('AccountPage mounted', { user, subscription, loading, error });
  }, [user, subscription, loading, error]);

  if (loading) {
    return <div>Загрузка...</div>;
  }

  return (
    <div className="container mx-auto p-6">
      <h1 className="text-3xl font-bold mb-6">Личный кабинет</h1>
      
      {/* Отображение подписки */}
      <div className="mb-6 p-4 bg-card rounded-lg">
        <h2 className="text-xl font-semibold mb-2">Подписка</h2>
        {error && <p className="text-destructive">Ошибка загрузки подписки</p>}
        {subscription ? (
          <div>
            <p>Тип: <span className="font-bold">{subscription.type}</span></p>
            <p>Статус: <span className="font-bold">{subscription.status}</span></p>
            <p>Действует до: {new Date(subscription.expiresAt).toLocaleDateString()}</p>
          </div>
        ) : (
          <p className="text-muted-foreground">У вас нет активной подписки</p>
        )}
      </div>

      {/* Вкладки */}
      <Tabs value={activeTab} onValueChange={setActiveTab}>
        <TabsList className="grid w-full grid-cols-4">
          <TabsTrigger value="overview">Обзор</TabsTrigger>
          <TabsTrigger value="purchases">Покупки</TabsTrigger>
          <TabsTrigger value="favorites">Избранное</TabsTrigger>
          <TabsTrigger value="settings">Настройки</TabsTrigger>
        </TabsList>
        
        <TabsContent value="overview">
          {/* Контент вкладки Обзор */}
        </TabsContent>
        
        <TabsContent value="purchases">
          {/* Контент вкладки Покупки */}
        </TabsContent>
        
        <TabsContent value="favorites">
          {/* Контент вкладки Избранное */}
        </TabsContent>
        
        <TabsContent value="settings">
          {/* Контент вкладки Настройки */}
        </TabsContent>
      </Tabs>
    </div>
  );
};
```

**Результат:**
- ✅ Все вкладки работают корректно
- ✅ Подписка отображается правильно
- ✅ Добавлены тесты для предотвращения регрессии

---

### 0.2 Замена тестовых данных (Фронтенд)
**Приоритет:** 🔥 КРИТИЧНО  
**Сложность:** S (2-3 дня)  
**Зависимости:** Нет

**Проблемы:**
- ❌ В коде присутствуют hardcoded тестовые данные
- ❌ Mock-данные в `src/lib/data.ts` используются вместо API
- ❌ Placeholder изображения и тексты

**Задачи:**
- [ ] **Аудит использования тестовых данных:**
  - Найти все места с mock-данными (поиск по `mock`, `dummy`, `test`, `placeholder`)
  - Составить список компонентов, использующих `src/lib/data.ts`
  - Проверить все страницы на наличие hardcoded данных

- [ ] **Замена на API вызовы:**
  - Заменить импорты из `data.ts` на вызовы `api.ts`
  - Убедиться, что все компоненты получают данные через API
  - Добавить loading states
  - Добавить error states

- [ ] **Замена placeholder контента:**
  - Заменить все `https://placeholder.com` на реальные изображения или CDN
  - Обновить тестовые тексты на осмысленные
  - Убрать "Lorem ipsum" заглушки

**Пример рефакторинга:**
```typescript
// ❌ БЫЛО (использование mock-данных):
// src/pages/ReleasesPage.tsx
import { releases } from '@/lib/data';

const ReleasesPage = () => {
  return (
    <div>
      {releases.map(release => (
        <ReleaseCard key={release.id} release={release} />
      ))}
    </div>
  );
};

// ✅ СТАЛО (использование API):
// src/pages/ReleasesPage.tsx
import { useEffect, useState } from 'react';
import { getReleases } from '@/lib/api';

const ReleasesPage = () => {
  const [releases, setReleases] = useState([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState(null);

  useEffect(() => {
    const fetchReleases = async () => {
      try {
        setLoading(true);
        const data = await getReleases();
        setReleases(data);
      } catch (err) {
        setError(err.message);
      } finally {
        setLoading(false);
      }
    };
    fetchReleases();
  }, []);

  if (loading) return <div>Загрузка...</div>;
  if (error) return <div>Ошибка: {error}</div>;

  return (
    <div>
      {releases.map(release => (
        <ReleaseCard key={release.id} release={release} />
      ))}
    </div>
  );
};
```

**Файлы для проверки:**
```
src/lib/data.ts           ← Удалить или пометить как deprecated
src/pages/*               ← Проверить все страницы
src/components/*          ← Проверить все компоненты
```

**Результат:**
- ✅ Все данные приходят из API
- ✅ Нет hardcoded mock-данных
- ✅ Реальные изображения и контент

---

## 🔴 ФАЗА 1: Критические улучшения (Must-Have)
**Цель:** Сделать проект готовым к реальной эксплуатации  
**Срок:** 4-6 недель

### 1.1 Интеграция реальной базы данных
**Приоритет:** 🔥 КРИТИЧНО  
**Сложность:** XL (3-4 недели)  
**Зависимости:** Нет

**Задачи:**
- [ ] Выбор БД: PostgreSQL (рекомендуется) или MongoDB
- [ ] Установка и настройка ORM/ODM:
  - Для PostgreSQL: **Prisma** (рекомендуется) или TypeORM
  - Для MongoDB: **Mongoose**
- [ ] Создание схемы базы данных на основе `backend/doc/schema.md`
- [ ] Миграция данных из in-memory массивов в БД
- [ ] Рефакторинг всех `*-db.ts` файлов для работы с ORM
- [ ] Настройка подключения к БД через переменные окружения
- [ ] Создание начальных миграций
- [ ] Разработка seed-скриптов для заполнения тестовыми данными
- [ ] **🆕 Замена всех тестовых данных бэкенда на реальные структуры**

**Дополнительные задачи (новое):**
- [ ] **Очистка тестовых данных в `backend/src/utils/*-db.ts`:**
  - Удалить hardcoded массивы с тестовыми данными
  - Создать seed-файлы для разработки (`prisma/seeds/`)
  - Разделить seeds на dev и production
  
- [ ] **Создание реалистичных seed-данных:**
  ```
  prisma/seeds/
  ├── dev-seed.ts          # Для локальной разработки
  ├── prod-seed.ts         # Минимальные данные для production
  ├── artists.seed.ts      # Реальные артисты
  ├── releases.seed.ts     # Реальные релизы
  └── users.seed.ts        # Тестовые пользователи (только dev)
  ```

**Пример Prisma Schema:**
```prisma
// prisma/schema.prisma

model User {
  id            String         @id @default(uuid())
  email         String         @unique
  password      String
  name          String
  role          Role           @default(FREE_USER)
  subscription  Subscription?
  createdAt     DateTime       @default(now())
  updatedAt     DateTime       @updatedAt
}

model Subscription {
  id          String    @id @default(uuid())
  userId      String    @unique
  user        User      @relation(fields: [userId], references: [id])
  type        SubType   // FREE, BASIC, PREMIUM
  status      SubStatus // ACTIVE, EXPIRED, CANCELLED
  startDate   DateTime
  expiresAt   DateTime
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
}

enum Role {
  ADMIN
  ARTIST
  PREMIUM_USER
  FREE_USER
}

enum SubType {
  FREE
  BASIC
  PREMIUM
}

enum SubStatus {
  ACTIVE
  EXPIRED
  CANCELLED
}
```

**Результат:**
- ✅ Данные сохраняются между перезапусками
- ✅ Готовая схема БД с миграциями
- ✅ Seed-скрипты для разработки
- ✅ **Нет тестовых заглушек в production коде**

---

### 1.2 Рефакторинг архитектуры бэкенда
**Приоритет:** 🔥 КРИТИЧНО  
**Сложность:** L (2-3 недели)  
**Зависимости:** 1.1

**Задачи:**
- [ ] Разделение монолитного `index.ts` на модули:
  ```
  backend/src/
  ├── controllers/     # Обработчики HTTP-запросов
  ├── services/        # Бизнес-логика
  ├── repositories/    # Работа с БД
  ├── middleware/      # Промежуточные обработчики
  ├── routes/          # Определение маршрутов
  ├── models/          # Модели данных (если не ORM)
  └── validators/      # Схемы валидации
  ```
- [ ] Создание контроллеров для каждого домена:
  - `auth.controller.ts`
  - `releases.controller.ts`
  - `artists.controller.ts`
  - `posts.controller.ts`
  - **🆕 `account.controller.ts` (для управления профилем)**
  - **🆕 `subscription.controller.ts` (для управления подписками)**
  - и т.д.
- [ ] Создание сервисного слоя для бизнес-логики
- [ ] Создание слоя репозиториев для работы с БД
- [ ] Настройка Dependency Injection (опционально, используя `tsyringe` или `inversify`)

**Результат:**
- ✅ Четкое разделение ответственности
- ✅ Легкая тестируемость
- ✅ Улучшенная поддерживаемость

---

### 1.3 Добавление валидации данных
**Приоритет:** 🔥 КРИТИЧНО  
**Сложность:** M (1-2 недели)  
**Зависимости:** 1.2

**Задачи:**
- [ ] Установка библиотеки валидации: **Zod** (рекомендуется) или Joi/Yup
- [ ] Создание схем валидации для всех API эндпоинтов
- [ ] Создание middleware для автоматической валидации
- [ ] Валидация входящих данных (body, query, params)
- [ ] Единообразная обработка ошибок валидации
- [ ] **🆕 Валидация данных для эндпоинта обновления профиля**

**Новые схемы валидации:**
```typescript
// validators/account.validator.ts
import { z } from 'zod';

export const updateProfileSchema = z.object({
  name: z.string().min(2).max(100).optional(),
  email: z.string().email().optional(),
  bio: z.string().max(500).optional(),
  avatar: z.string().url().optional(),
  socialLinks: z.object({
    instagram: z.string().url().optional(),
    twitter: z.string().url().optional(),
    spotify: z.string().url().optional(),
  }).optional(),
});

export const changePasswordSchema = z.object({
  currentPassword: z.string().min(6),
  newPassword: z.string().min(8).regex(/^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)/),
  confirmPassword: z.string(),
}).refine((data) => data.newPassword === data.confirmPassword, {
  message: "Пароли не совпадают",
  path: ["confirmPassword"],
});
```

**Результат:**
- ✅ Защита от некорректных данных
- ✅ Четкие error messages для клиента
- ✅ Повышение безопасности

---

### 1.4 Базовое тестирование бэкенда
**Приоритет:** 🔥 КРИТИЧНО  
**Сложность:** L (2-3 недели)  
**Зависимости:** 1.2

**Задачи:**
- [ ] Настройка тестового окружения: **Jest** + **Supertest**
- [ ] Настройка тестовой БД (отдельная от dev/prod)
- [ ] Написание unit-тестов для сервисов (минимум 70% покрытия)
- [ ] Написание integration-тестов для API эндпоинтов
- [ ] **🆕 Тесты для эндпоинтов личного кабинета**
- [ ] **🆕 Тесты для обновления профиля**
- [ ] Настройка CI для автоматического запуска тестов
- [ ] Добавление тестов в pre-commit hooks

**Новые тесты:**
```typescript
// __tests__/account.controller.test.ts
describe('Account Controller', () => {
  describe('GET /api/account/profile', () => {
    it('should return user profile with subscription', async () => {
      const response = await request(app)
        .get('/api/account/profile')
        .set('Authorization', `Bearer ${validToken}`);
      
      expect(response.status).toBe(200);
      expect(response.body).toHaveProperty('user');
      expect(response.body).toHaveProperty('subscription');
    });

    it('should return 401 without token', async () => {
      const response = await request(app)
        .get('/api/account/profile');
      
      expect(response.status).toBe(401);
    });
  });

  describe('PUT /api/account/profile', () => {
    it('should update user profile', async () => {
      const response = await request(app)
        .put('/api/account/profile')
        .set('Authorization', `Bearer ${validToken}`)
        .send({ name: 'New Name' });
      
      expect(response.status).toBe(200);
      expect(response.body.name).toBe('New Name');
    });
  });
});
```

**Результат:**
- ✅ Покрытие бэкенда тестами ≥70%
- ✅ Автоматическая проверка при каждом коммите
- ✅ Снижение количества багов

---

### 1.5 Система логирования
**Приоритет:** 🔥 КРИТИЧНО  
**Сложность:** M (1 неделя)  
**Зависимости:** Нет

**Задачи:**
- [ ] Установка библиотеки логирования: **Winston** или **Pino**
- [ ] Настройка разных уровней логирования (error, warn, info, debug)
- [ ] Логирование всех HTTP-запросов (middleware)
- [ ] Логирование ошибок с stack traces
- [ ] Настройка ротации логов
- [ ] Разные конфигурации для dev/prod окружений
- [ ] **🆕 Логирование действий в личном кабинете**
- [ ] **🆕 Аудит изменений профиля**

**Результат:**
- ✅ Полная видимость работы приложения
- ✅ Упрощение отладки проблем
- ✅ Возможность аудита действий пользователей

---

## 🟡 ФАЗА 2: Важные улучшения (Should-Have)
**Цель:** Повышение безопасности и UX  
**Срок:** 4-5 недель

### 2.1 Функционал "Настройки аккаунта"
**Приоритет:** 🟠 ВЫСОКИЙ  
**Сложность:** M (1-2 недели)  
**Зависимости:** 1.1, 1.2, 1.3

**Описание:**
Создание полноценного раздела настроек для редактирования данных аккаунта.

**Задачи (Backend):**
- [ ] **Создание API эндпоинтов:**
  ```typescript
  GET    /api/account/profile          # Получить данные профиля
  PUT    /api/account/profile          # Обновить профиль
  PUT    /api/account/password         # Изменить пароль
  PUT    /api/account/avatar           # Загрузить аватар
  DELETE /api/account                  # Удалить аккаунт
  GET    /api/account/sessions         # Активные сессии
  DELETE /api/account/sessions/:id     # Завершить сессию
  ```

- [ ] **Реализация AccountController:**
  ```typescript
  // controllers/account.controller.ts
  export class AccountController {
    async getProfile(req: Request, res: Response) {
      const userId = req.user.id;
      const profile = await this.accountService.getProfile(userId);
      res.json(profile);
    }

    async updateProfile(req: Request, res: Response) {
      const userId = req.user.id;
      const updates = req.body;
      const updated = await this.accountService.updateProfile(userId, updates);
      res.json(updated);
    }

    async changePassword(req: Request, res: Response) {
      const userId = req.user.id;
      const { currentPassword, newPassword } = req.body;
      await this.accountService.changePassword(userId, currentPassword, newPassword);
      res.json({ message: 'Пароль успешно изменён' });
    }

    async deleteAccount(req: Request, res: Response) {
      const userId = req.user.id;
      await this.accountService.deleteAccount(userId);
      res.json({ message: 'Аккаунт удалён' });
    }
  }
  ```

- [ ] **Реализация AccountService:**
  - Бизнес-логика обновления профиля
  - Проверка текущего пароля перед сменой
  - Хеширование нового пароля
  - Soft delete аккаунта (опционально)

**Задачи (Frontend):**
- [ ] **Создание страницы настроек:**
  ```
  src/pages/
  └── settings/
      ├── SettingsPage.tsx           # Главная страница настроек
      ├── ProfileSettings.tsx        # Редактирование профиля
      ├── SecuritySettings.tsx       # Безопасность
      ├── SubscriptionSettings.tsx   # Управление подпиской
      └── PrivacySettings.tsx        # Приватность
  ```

- [ ] **Компоненты настроек:**
  ```typescript
  // src/pages/settings/ProfileSettings.tsx
  const ProfileSettings = () => {
    const user = useStore((state) => state.user);
    const [formData, setFormData] = useState({
      name: user?.name || '',
      email: user?.email || '',
      bio: user?.bio || '',
    });

    const handleSubmit = async (e) => {
      e.preventDefault();
      try {
        await updateProfile(formData);
        toast.success('Профиль обновлён');
      } catch (error) {
        toast.error('Ошибка обновления');
      }
    };

    return (
      <form onSubmit={handleSubmit}>
        <div className="space-y-4">
          <Input
            label="Имя"
            value={formData.name}
            onChange={(e) => setFormData({ ...formData, name: e.target.value })}
          />
          <Input
            label="Email"
            type="email"
            value={formData.email}
            onChange={(e) => setFormData({ ...formData, email: e.target.value })}
          />
          <Textarea
            label="О себе"
            value={formData.bio}
            onChange={(e) => setFormData({ ...formData, bio: e.target.value })}
          />
          <Button type="submit">Сохранить изменения</Button>
        </div>
      </form>
    );
  };
  ```

- [ ] **Добавление вкладки "Настройки" в AccountPage:**
  ```typescript
  // src/pages/AccountPage.tsx
  <TabsContent value="settings">
    <SettingsPage />
  </TabsContent>
  ```

- [ ] **Функции API:**
  ```typescript
  // src/lib/api.ts
  export const updateProfile = async (data: UpdateProfileDto) => {
    const response = await apiClient.put('/api/account/profile', data);
    return response.data;
  };

  export const changePassword = async (data: ChangePasswordDto) => {
    const response = await apiClient.put('/api/account/password', data);
    return response.data;
  };

  export const deleteAccount = async () => {
    const response = await apiClient.delete('/api/account');
    return response.data;
  };
  ```

**Дополнительные возможности:**
- [ ] Загрузка и обрезка аватара
- [ ] Предпросмотр изменений перед сохранением
- [ ] История изменений профиля
- [ ] Подтверждение email при смене
- [ ] 2FA (двухфакторная аутентификация)

**UI/UX требования:**
- ✅ Валидация на стороне клиента (Zod)
- ✅ Debounce для проверки уникальности email
- ✅ Индикаторы прогресса при сохранении
- ✅ Подтверждение при удалении аккаунта
- ✅ Toast-уведомления об успехе/ошибке

**Результат:**
- ✅ Полноценный раздел настроек
- ✅ Пользователи могут редактировать профиль
- ✅ Безопасная смена пароля
- ✅ Управление активными сессиями

---

### 2.2 Refresh-токены и улучшенная аутентификация
**Приоритет:** 🟠 ВЫСОКИЙ  
**Сложность:** M (1-2 недели)  
**Зависимости:** 1.1

**Задачи:**
- [ ] Создание таблицы `refresh_tokens` в БД
- [ ] Генерация и хранение refresh-токенов
- [ ] Эндпоинт `/api/auth/refresh` для обновления access-токена
- [ ] Механизм автоматического обновления токена на фронтенде
- [ ] Механизм отзыва refresh-токенов (logout)
- [ ] Хранение refresh-токена в httpOnly cookies (безопаснее)

**Результат:**
- ✅ Пользователи остаются залогинены длительное время
- ✅ Повышенная безопасность
- ✅ Улучшенный UX

---

### 2.3 Ролевая модель доступа (RBAC)
**Приоритет:** 🟠 ВЫСОКИЙ  
**Сложность:** L (2-3 недели)  
**Зависимости:** 1.1, 1.2

**Задачи:**
- [ ] Создание таблиц `roles` и `permissions`
- [ ] Определение ролей: `admin`, `artist`, `premium_user`, `free_user`
- [ ] Определение прав доступа для каждой роли
- [ ] Создание middleware для проверки прав доступа
- [ ] Рефакторинг эндпоинтов с учетом ролей
- [ ] Обновление фронтенда для поддержки ролей

**Результат:**
- ✅ Гибкое управление доступом
- ✅ Безопасное разграничение функций
- ✅ Возможность добавления новых ролей

---

### 2.4 Обработка загрузки файлов
**Приоритет:** 🟠 ВЫСОКИЙ  
**Сложность:** L (2 недели)  
**Зависимости:** 1.1, 1.2

**Задачи:**
- [ ] Выбор хранилища: AWS S3, Cloudinary или локальное хранилище
- [ ] Установка библиотеки: **Multer** для обработки multipart/form-data
- [ ] Эндпоинт для загрузки аудио-файлов
- [ ] Эндпоинт для загрузки обложек
- [ ] **🆕 Эндпоинт для загрузки аватаров пользователей**
- [ ] Валидация файлов (тип, размер, формат)
- [ ] Генерация превью для изображений
- [ ] Обработка прогресса загрузки на фронтенде

**Новые эндпоинты:**
```typescript
POST /api/upload/avatar        # Загрузка аватара
POST /api/upload/cover         # Загрузка обложки релиза
POST /api/upload/audio         # Загрузка аудио-файла
```

**Результат:**
- ✅ Возможность загрузки музыки и обложек
- ✅ Загрузка аватаров пользователей
- ✅ Оптимизация хранения медиа-контента
- ✅ Улучшенный UX при загрузке

---

### 2.5 CI/CD для бэкенда
**Приоритет:** 🟠 ВЫСОКИЙ  
**Сложность:** M (1-2 недели)  
**Зависимости:** 1.4

**Задачи:**
- [ ] Создание `.github/workflows/backend-ci.yml`
- [ ] Автоматический запуск тестов при PR
- [ ] Автоматическая сборка Docker-образа
- [ ] Деплой на staging окружение
- [ ] Настройка автоматического деплоя в production (по тегам)
- [ ] Настройка уведомлений об ошибках деплоя

**Результат:**
- ✅ Автоматизация развертывания
- ✅ Снижение human errors
- ✅ Быстрая доставка фич в production

---

### 2.6 Единая обработка ошибок
**Приоритет:** 🟠 ВЫСОКИЙ  
**Сложность:** S (3-5 дней)  
**Зависимости:** 1.2, 1.5

**Задачи:**
- [ ] Создание кастомных классов ошибок
- [ ] Глобальный error handler middleware
- [ ] Единообразный формат ответов с ошибками
- [ ] Интеграция с логированием
- [ ] Обработка необработанных промисов и исключений

**Результат:**
- ✅ Единообразные error responses
- ✅ Упрощение отладки
- ✅ Улучшенный DX

---

## 🟢 ФАЗА 3: Желательные улучшения (Nice-to-Have)
**Цель:** Повышение производительности и UX  
**Срок:** 4-5 недель

### 3.1 Визуальные доработки UI
**Приоритет:** 🟢 СРЕДНИЙ  
**Сложность:** M (1-2 недели)  
**Зависимости:** 0.1, 2.1

**Описание:**
Улучшение визуального дизайна и пользовательского опыта на основе обратной связи.

**Задачи:**
- [ ] **Аудит текущего дизайна:**
  - Составить список визуальных проблем
  - Собрать feedback от пользователей/тестировщиков
  - Проанализировать конкурентов
  - Создать список приоритетных улучшений

- [ ] **Улучшения личного кабинета:**
  - Редизайн карточек подписки
  - Улучшение таблицы покупок
  - Добавление прогресс-баров
  - Улучшение мобильной версии
  - Добавление анимаций переходов

- [ ] **Общие улучшения:**
  - Единый spacing и typography
  - Улучшение color scheme (темная/светлая тема)
  - Добавление микроанимаций
  - Улучшение состояний (hover, active, focus)
  - Оптимизация для accessibility (WCAG 2.1)

- [ ] **Компонентная библиотека:**
  - Документирование всех UI компонентов
  - Создание Storybook (опционально)
  - Единые стили для форм
  - Единые стили для кнопок и карточек

**Конкретные улучшения:**
```typescript
// src/components/SubscriptionCard.tsx - пример улучшения
const SubscriptionCard = ({ subscription }) => {
  const daysLeft = calculateDaysLeft(subscription.expiresAt);
  const progressPercent = calculateProgress(subscription);

  return (
    <Card className="relative overflow-hidden">
      {/* Градиентный фон в зависимости от типа подписки */}
      <div className={cn(
        "absolute inset-0 opacity-5",
        subscription.type === 'PREMIUM' && "bg-gradient-to-br from-purple-500 to-pink-500",
        subscription.type === 'BASIC' && "bg-gradient-to-br from-blue-500 to-cyan-500"
      )} />
      
      <CardHeader>
        <div className="flex items-center justify-between">
          <CardTitle className="flex items-center gap-2">
            {subscription.type === 'PREMIUM' && <Crown className="w-5 h-5 text-yellow-500" />}
            {subscription.type}
          </CardTitle>
          <Badge variant={subscription.status === 'ACTIVE' ? 'success' : 'destructive'}>
            {subscription.status}
          </Badge>
        </div>
      </CardHeader>
      
      <CardContent className="space-y-4">
        {/* Прогресс-бар */}
        <div>
          <div className="flex justify-between text-sm mb-2">
            <span>Осталось дней</span>
            <span className="font-bold">{daysLeft}</span>
          </div>
          <Progress value={progressPercent} className="h-2" />
        </div>
        
        {/* Дата окончания */}
        <div className="flex items-center gap-2 text-sm text-muted-foreground">
          <Calendar className="w-4 h-4" />
          <span>Активна до {formatDate(subscription.expiresAt)}</span>
        </div>
      </CardContent>
      
      <CardFooter>
        <Button variant="outline" className="w-full">
          Управление подпиской
        </Button>
      </CardFooter>
    </Card>
  );
};
```

**Планируемые доработки (список можно дополнять):**
- [ ] Редизайн карточек релизов
- [ ] Улучшение аудиоплеера
- [ ] Добавление skeleton loaders
- [ ] Улучшение форм (inline validation, лучший UX)
- [ ] Добавление empty states
- [ ] Улучшение модальных окон
- [ ] Анимация появления элементов
- [ ] Адаптивная типографика
- [ ] Оптимизация для планшетов

**Инструменты:**
- Figma/Sketch для прототипирования
- Tailwind CSS для стилизации
- Framer Motion для анимаций
- Radix UI для accessibility

**Результат:**
- ✅ Современный и привлекательный UI
- ✅ Улучшенный UX
- ✅ Единообразный дизайн
- ✅ Лучшая accessibility

---

### 3.2 Кэширование
**Приоритет:** 🟢 СРЕДНИЙ  
**Сложность:** M (1-2 недели)  
**Зависимости:** 1.1

**Задачи:**
- [ ] Установка **Redis**
- [ ] Кэширование часто запрашиваемых данных (релизы, артисты)
- [ ] Настройка TTL для разных типов данных
- [ ] Инвалидация кэша при обновлении данных
- [ ] Кэширование на уровне HTTP (Cache-Control headers)

**Результат:**
- ✅ Ускорение работы API в 3-5 раз
- ✅ Снижение нагрузки на БД

---

### 3.3 Rate Limiting и защита от DDoS
**Приоритет:** 🟢 СРЕДНИЙ  
**Сложность:** S (2-3 дня)  
**Зависимости:** Нет

**Задачи:**
- [ ] Установка `express-rate-limit`
- [ ] Настройка лимитов для разных эндпоинтов
- [ ] Более жесткие лимиты для auth-эндпоинтов
- [ ] IP-based blocking
- [ ] Интеграция с Redis для distributed rate limiting

**Результат:**
- ✅ Защита от брутфорса
- ✅ Защита от DDoS
- ✅ Стабильность сервиса

---

### 3.4 API Documentation (Swagger/OpenAPI)
**Приоритет:** 🟢 СРЕДНИЙ  
**Сложность:** M (1 неделя)  
**Зависимости:** 1.2

**Задачи:**
- [ ] Установка `swagger-jsdoc` и `swagger-ui-express`
- [ ] Документирование всех эндпоинтов
- [ ] Добавление примеров запросов/ответов
- [ ] Автогенерация схем из Zod/Joi валидаторов
- [ ] Размещение документации на `/api-docs`

**Результат:**
- ✅ Интерактивная API документация
- ✅ Упрощение onboarding разработчиков
- ✅ Возможность тестирования API из браузера

---

### 3.5 Мониторинг и метрики
**Приоритет:** 🟢 СРЕДНИЙ  
**Сложность:** M (1-2 недели)  
**Зависимости:** 1.5

**Задачи:**
- [ ] Установка **Prometheus** для сбора метрик
- [ ] Установка **Grafana** для визуализации
- [ ] Сбор метрик: response time, error rate, request count
- [ ] Настройка алертов (email/Slack)
- [ ] Мониторинг состояния БД и Redis

**Результат:**
- ✅ Проактивное выявление проблем
- ✅ Визуализация производительности
- ✅ Data-driven оптимизация

---

### 3.6 Улучшения фронтенда

#### 3.6.1 Добавление E2E тестов
**Сложность:** M (1 неделя)
- [ ] Установка **Playwright** или **Cypress**
- [ ] Написание E2E тестов для критических user flows
- [ ] Тест: регистрация → вход → покупка подписки → доступ к контенту
- [ ] **🆕 Тест: редактирование профиля в настройках**
- [ ] **🆕 Тест: переключение между вкладками личного кабинета**
- [ ] Интеграция в CI

#### 3.6.2 Оптимизация производительности
**Сложность:** M (1 неделя)
- [ ] Code splitting по роутам
- [ ] Lazy loading компонентов
- [ ] Оптимизация бандла (анализ с `vite-bundle-visualizer`)
- [ ] Виртуализация списков (react-window)
- [ ] Оптимизация изображений (WebP, lazy loading)

#### 3.6.3 Улучшение UX
**Сложность:** S (3-5 дней)
- [ ] Скелетоны вместо спиннеров
- [ ] Оптимистичные обновления UI
- [ ] Offline-режим с Service Worker
- [ ] Progressive Web App (PWA)
- [ ] **🆕 Unsaved changes warning при выходе из настроек**
- [ ] **🆕 Автосохранение черновиков (debounced)**

---

## 🔵 ФАЗА 4: Долгосрочные улучшения (Future)
**Цель:** Масштабируемость и продвинутые фичи  
**Срок:** 2-3 месяца

### 4.1 Микросервисная архитектура
**Сложность:** XXL (1-2 месяца)

**Задачи:**
- [ ] Разделение на микросервисы:
  - Auth Service
  - Content Service (releases, artists, posts)
  - **🆕 User Profile Service (управление профилями)**
  - Payment Service
  - Notification Service
- [ ] API Gateway (Kong, Express Gateway)
- [ ] Message broker (RabbitMQ, Kafka)
- [ ] Service discovery (Consul, Eureka)

---

### 4.2 Real-time функционал
**Сложность:** L (2-3 недели)

**Задачи:**
- [ ] WebSocket сервер (Socket.io)
- [ ] Live-уведомления о новых релизах
- [ ] Live-чат с поддержкой
- [ ] Real-time обновление голосований в опросах
- [ ] **🆕 Live-уведомления об изменениях в аккаунте**
- [ ] **🆕 Показ онлайн-статуса пользователей**

---

### 4.3 Расширенная аналитика
**Сложность:** L (2-3 недели)

**Задачи:**
- [ ] Интеграция Google Analytics / Mixpanel
- [ ] Трекинг пользовательского поведения
- [ ] A/B тестирование
- [ ] Дашборд для артистов (статистика прослушиваний)
- [ ] **🆕 Аналитика использования настроек аккаунта**
- [ ] **🆕 Heatmap взаимодействия с UI**

---

### 4.4 Платежная система
**Сложность:** XL (3-4 недели)

**Задачи:**
- [ ] Интеграция Stripe/PayPal
- [ ] Обработка подписок и разовых платежей
- [ ] Webhooks для обработки платежей
- [ ] Страница с историей транзакций

---

### 4.5 Admin панель
**Сложность:** L (2-3 недели)

**Задачи:**
- [ ] Отдельное приложение для админов
- [ ] CRUD для всех сущностей
- [ ] Управление пользователями и ролями
- [ ] Модерация контента
- [ ] Статистика и аналитика
- [ ] **🆕 Просмотр и модерация профилей пользователей**
- [ ] **🆕 История изменений профилей (audit log)**

---

## 📈 Обновлённая приоритизация задач

### Критический путь (MVP для продакшна):
```
ФАЗА 0 (Hotfix):
0.1 (Баг личного кабинета) → 0.2 (Замена тестовых данных)
    ↓
ФАЗА 1 (Critical):
1.1 (БД + замена тестовых данных) → 1.2 (Архитектура) → 1.3 (Валидация) → 1.4 (Тесты) → 1.5 (Логи)
    ↓
ФАЗА 2 (Important):
2.1 (Настройки аккаунта) → 2.2 (Refresh-токены) → 2.3 (RBAC) → 2.4 (Загрузка файлов) → 2.5 (CI/CD)
    ↓
ФАЗА 3 (Nice-to-have):
3.1 (Визуальные доработки) → 3.2-3.6 (Оптимизации)
```

### Параллельные задачи:
- **0.1** и **0.2** можно делать одновременно
- **1.5** (Логи) - независимая задача, можно начать раньше
- **2.5** (CI/CD) - после 1.4
- **3.3** (Rate Limiting) - независимая задача
- **3.1** (Визуальные доработки) - можно делать параллельно с backend-задачами

### Зависимости новых задач:
```
0.1 (Баг личного кабинета)
  └─> 2.1 (Настройки аккаунта)
      └─> 3.1 (Визуальные доработки)

1.1 (БД)
  └─> 0.2 (Замена тестовых данных бэкенда)
  └─> 2.1 (Настройки аккаунта - backend)
```

---

## 📊 Метрики успеха (обновлённые)

### После Фазы 0 (Hotfix):
- ✅ Все вкладки личного кабинета работают
- ✅ Подписка отображается корректно
- ✅ Нет критических ошибок в console
- ✅ Все mock-данные заменены на API вызовы (фронтенд)

### После Фазы 1:
- ✅ Данные сохраняются в БД
- ✅ Покрытие тестами ≥70%
- ✅ Нет critical issues в коде
- ✅ Нет тестовых данных в production коде

### После Фазы 2:
- ✅ Безопасная аутентификация с refresh-токенами
- ✅ Работающий деплой pipeline
- ✅ Загрузка файлов (аватары, обложки, аудио)
- ✅ **Функционал настроек аккаунта полностью работает**
- ✅ **Пользователи могут редактировать профиль**

### После Фазы 3:
- ✅ API response time < 200ms (95 перцентиль)
- ✅ Uptime ≥ 99.5%
- ✅ Полная API документация
- ✅ **Современный и привлекательный UI**
- ✅ **Lighthouse Score > 90**

---

## 🛠️ Инструменты и технологии (обновлённые)

### Backend Tech Stack (рекомендуемый):
- **БД:** PostgreSQL + Prisma
- **Валидация:** Zod
- **Тестирование:** Jest + Supertest
- **Логирование:** Winston
- **Загрузка файлов:** Multer + AWS S3/Cloudinary
- **Кэш:** Redis
- **Мониторинг:** Prometheus + Grafana
- **Контейнеризация:** Docker + Docker Compose

### Frontend Tech Stack (существующий + дополнения):
- **React 18** + **Vite**
- **Tailwind CSS** + **shadcn/ui**
- **Zustand** (state management)
- **React Hook Form** (для форм в настройках)
- **Zod** (валидация на клиенте)
- **React Query/SWR** (опционально, для кэширования запросов)
- **Framer Motion** (анимации)

### DevOps:
- **CI/CD:** GitHub Actions
- **Хостинг бэкенда:** Railway, Render, DigitalOcean
- **Хостинг фронтенда:** Vercel, Netlify
- **Хостинг БД:** Supabase, PlanetScale, Railway
- **Хранилище файлов:** AWS S3, Cloudinary, DigitalOcean Spaces

---

## 📅 Временная шкала (обновлённая)

| Фаза | Срок | Ключевые задачи |
|------|------|-----------------|
| **Фаза 0** | 1-2 недели | Багфикс личного кабинета, замена тестовых данных (frontend) |
| **Фаза 1** | 4-6 недель | БД, архитектура, валидация, тесты, логи, замена тестовых данных (backend) |
| **Фаза 2** | 4-5 недель | Настройки аккаунта, refresh-токены, RBAC, загрузка файлов, CI/CD |
| **Фаза 3** | 4-5 недель | Визуальные доработки, кэш, rate limiting, API docs, мониторинг, E2E тесты |
| **Фаза 4** | 2-3 месяца | Микросервисы, real-time, аналитика, платежи, admin панель |

**Общая продолжительность до production-ready:** 3-4 месяца (Фазы 0-2)  
**До полнофункционального продукта:** 5-7 месяцев (Фазы 0-4)

---

## 🎯 Рекомендации по началу работы

### Неделя 1-2 (ФАЗА 0 - Hotfix):
1. **День 1-3:** Исправление личного кабинета
   - Диагностика и фикс вкладок
   - Исправление отображения подписки
   - Тестирование

2. **День 4-7:** Замена тестовых данных на фронтенде
   - Аудит mock-данных
   - Замена на API вызовы
   - Тестирование всех страниц

3. **День 8-10:** Code review и тестирование
   - Проверка всех изменений
   - Исправление найденных багов
   - Подготовка к релизу

### Неделя 3-8 (ФАЗА 1):
Начать с задачи **1.1 (Интеграция БД)** - это фундамент!

### Совет для junior-разработчиков:
- Начните с **Фазы 0** - это небольшие, конкретные задачи
- Каждую задачу разбивайте на подзадачи по 2-4 часа
- Делайте частые коммиты с понятными сообщениями
- Не стесняйтесь задавать вопросы в команде
- Используйте этот roadmap как чеклист

---

## ✅ Чеклист для Product Owner / Team Lead

### Готовность к продакшну:
- [ ] Фаза 0 полностью завершена
- [ ] Фаза 1 полностью завершена
- [ ] Фаза 2 завершена минимум на 80%
- [ ] Все критические баги исправлены
- [ ] Покрытие тестами ≥ 70%
- [ ] Security audit пройден
- [ ] Performance testing пройден
- [ ] Настроен мониторинг и алерты
- [ ] Документация актуализирована
- [ ] Настроен CI/CD pipeline

**Удачи в разработке! 🚀**