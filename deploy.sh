#!/bin/bash

# 🚀 Скрипт быстрого деплоя на Vercel

echo "🚀 Начинаем деплой на Vercel..."
echo ""

# Проверка установки Vercel CLI
if ! command -v vercel &> /dev/null; then
    echo "❌ Vercel CLI не установлен"
    echo "📦 Установите его командой: npm i -g vercel"
    exit 1
fi

echo "✅ Vercel CLI найден"
echo ""

# Проверка тестов (опционально)
read -p "🧪 Запустить тесты перед деплоем? (y/n) " -n 1 -r
echo
if [[ $REPLY =~ ^[Yy]$ ]]; then
    echo "🧪 Запускаем тесты..."
    npm test
    if [ $? -ne 0 ]; then
        echo "❌ Тесты не прошли. Деплой отменен."
        exit 1
    fi
    echo "✅ Тесты пройдены"
fi

echo ""
echo "📦 Создаем production билд..."
npm run build

if [ $? -ne 0 ]; then
    echo "❌ Ошибка при сборке проекта"
    exit 1
fi

echo "✅ Билд успешно создан"
echo ""

# Выбор типа деплоя
read -p "🎯 Production деплой? (y - production, n - preview) " -n 1 -r
echo
if [[ $REPLY =~ ^[Yy]$ ]]; then
    echo "🚀 Деплоим в production..."
    vercel --prod
else
    echo "🚀 Создаем preview деплой..."
    vercel
fi

echo ""
echo "✅ Деплой завершен!"
echo ""
echo "📝 Не забудьте:"
echo "   1. Настроить переменные окружения в Vercel Dashboard"
echo "   2. Применить миграции БД (см. VERCEL_DEPLOY.md)"
echo "   3. Запустить seed для тестовых данных"
echo ""
echo "🎉 Готово!"
