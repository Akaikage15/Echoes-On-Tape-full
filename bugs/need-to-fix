# Отчёт о проблемах и дорожная карта по исправлению после редизайна (с particles.js)

## ❌ Обзор проблемы

После внедрения анимированного фона с частицами (`ParticlesBackground`) и применения эффекта "Liquid Glass" на некоторые элементы возникли критические проблемы с функциональностью и визуальным наложением, затрагивающие интерактивность и внешний вид сайта.

## 📝 Детальное описание проблем

1.  **Проблемы с кликабельностью (невозможность взаимодействия):**
    *   **Элементы навигации:** Части футера (ссылки, навигационные элементы), элементы "Часто задаваемых вопросов" и ответов на них стали некликабельными.
    *   **Фильтрация релизов:** Механизм фильтрации на странице релизов (`/releases`) не реагирует на клики, делая функционал бесполезным.
    *   **Причина:** Вероятно, компонент `ParticlesBackground`, который расположен абсолютно и занимает всю видимую область, перехватывает все события мыши, не позволяя им достигать нижележащих элементов.

2.  **Проблемы с Z-индексом (слои наложения):**
    *   **Футер:** Анимированные частицы отображаются поверх футера, хотя футер должен быть поверх фоновых элементов. Это нарушает визуальную иерархию.
    *   **Другие элементы:** Возможно, аналогичные проблемы могут проявляться и с другими элементами, которые должны быть поверх фона (например, некоторые секции страниц).

3.  **Визуальные проблемы ("просвечивание" частиц):**
    *   **Основные элементы страниц:** Частицы просвечивают сквозь некоторые основные элементы страниц, которые должны быть непрозрачными. Это касается не только "Liquid Glass" элементов, но и других секцию.
    *   **Liquid Glass на карточках:** На карточках (релизы, артисты, блог-посты), где применён эффект "Liquid Glass" (полупрозрачный фон с `backdrop-blur`), частицы видны. Это ожидаемое поведение для Liquid Glass, но если предполагалось, что они не должны просвечивать *вообще* (даже через размытие), это стоит уточнить.

## 🔬 Анализ корневых причин (гипотеза)

*   Основная причина проблем с кликабельностью и Z-индексом заключается в том, что компонент `ParticlesBackground`, вероятно, не имеет свойства `pointer-events: none;` или некорректно управляет своим `z-index` относительно других элементов. Если `ParticlesBackground` находится выше других интерактивных элементов в DOM и перехватывает события, то они не дойдут до целевых элементов.
*   Проблемы просвечивания вызваны комбинацией недостаточного `z-index` у "передних" элементов и/или их недостаточной непрозрачностью. Для "Liquid Glass" элементов просвечивание является частью эффекта, но для других элементов это может быть ошибкой.

## 🗺️ Дорожная карта по исправлению (Roadmap)

1.  **Исправление кликабельности и Z-индекса для `ParticlesBackground`:**
    *   **Действие:** Добавить CSS-свойство `pointer-events: none;` к корневому элементу `ParticlesBackground`.
    *   **Обоснование:** Это позволит событиям мыши проходить сквозь частицы к элементам, расположенным под ними, решая проблему некликабельности.
    *   **Побочный эффект:** Интерактивность самих частиц (например, реакция на наведение или клик) будет отключена. Если такая интерактивность критична, потребуется более сложное решение (например, использование двух слоёв частиц, один интерактивный, другой нет, или более тонкое управление `pointer-events` с помощью JS).
    *   **Дополнительно:** Убедиться, что `z-index` у `ParticlesBackground` установлен как `z-0` или отрицательный, а у всех интерактивных и важных элементов контента (`Header`, `Footer`, `main`, модальные окна и т.д.) `z-index` выше (например, `z-10`, `z-20`).

2.  **Коррекция визуальных проблем:**
    *   **Действие:** Проверить `z-index` и `background-color` для всех элементов, сквозь которые не должны просвечивать частицы.
    *   **Обоснование:** Убедиться, что у футера, основных контентных блоков и других элементов, которые должны быть непрозрачными, есть достаточный `z-index` (например, `z-10` или выше) и полностью непрозрачный фон (например, `bg-background` без слэша для прозрачности).
    *   **Уточнение по Liquid Glass:** Если Liquid Glass на карточках не должен показывать фон вообще (то есть, размытие должно быть "пустым" без частиц), то эффект `backdrop-blur` должен быть применён к элементу, который сам по себе является фоном, а карточки должны иметь непрозрачный фон. Однако, текущая реализация Liquid Glass (полупрозрачный фон + `backdrop-blur`) подразумевает, что он будет показывать то, что *под ним*. Если это нежелательно, нужно будет пересмотреть реализацию Liquid Glass для карточек.

3.  **Тестирование:**
    *   Проверить все ссылки, кнопки и интерактивные элементы на всех страницах.
    *   Визуально оценить правильность наложения слоёв на всех страницах, особенно футера и основных контентных блоков.

---
`[X] Создание файла с отчётом о багах.`